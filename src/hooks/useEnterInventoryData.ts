/** Inventory count read/query orchestration — see `useInventoryCountData` in the inventory-count feature. */
export { useInventoryCountData as useEnterInventoryData } from "@/features/inventory-count/hooks/useInventoryCountData";
