/**
 * Route shell for the inventory count hub + session editor.
 * UI composition and data hooks live under `src/features/inventory-count/`.
 */
import InventoryCountPage from "@/features/inventory-count/pages/InventoryCountPage";

export default function EnterInventory() {
  return <InventoryCountPage />;
}
